import { Router } from 'express';

const router = Router();

// Placeholder for position routes
router.get('/', (req, res) => {
  res.json({ message: 'Position routes - Coming soon' });
});

export default router;
