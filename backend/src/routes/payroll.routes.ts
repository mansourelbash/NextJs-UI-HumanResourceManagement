import { Router } from 'express';

const router = Router();

// Placeholder for payroll routes
router.get('/', (req, res) => {
  res.json({ message: 'Payroll routes - Coming soon' });
});

export default router;
