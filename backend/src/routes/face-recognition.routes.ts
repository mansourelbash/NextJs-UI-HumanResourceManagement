import { Router } from 'express';

const router = Router();

// Placeholder for face recognition routes
router.get('/', (req, res) => {
  res.json({ message: 'Face recognition routes - Coming soon' });
});

export default router;
